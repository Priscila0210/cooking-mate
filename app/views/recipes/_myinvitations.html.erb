
<div class="booking-list">
  <% @my_requests.not_declined.each do |booking| %>
    <div class="booking-card text-center">
      <h4 class="booking-status m-2"><strong id="color-green"><%= booking.user.nickname %></strong> would like to cook <strong id="color-green"><%= booking.recipe.title %></strong></h4>
      <h4 class="booking-status m-2"><strong> on:</strong> <%= booking.date %></h4>
      <div class="d-flex justify-content-around align-items-center">
        <% if booking.status == 'pending' %>
          <%= link_to "Accept" , accepted_path(booking),class: "btn btn-sm btn-ghost-solid", data: { turbo_method: :patch } %>
          <%= link_to "Decline", declined_path(booking),class: "btn btn-sm btn-ghost-solid", data: { turbo_method: :patch } %>
        <% end %>
      </div>
      <div class="text-center">
        <% if booking.accepted? %>
          <%= link_to "chat", chatroom_path(booking.chatroom), class: "btn btn-sm btn-ghost-solid"%>
        <% end %>
      </div>
    </div>
  <% end %>
  <% if @my_requests.not_declined.empty? %>
    Don't worry, your cooking mate is on its way!
  <% end %>
</div>



<!--
<div class= "request-list">
  <% @my_requests.each do |booking| %>
    <h3><%= booking.user.nickname %></h3>
    <h3><%= booking.date %></h3>
    <h3><%= booking.status%></h3>
  <% end %>
</div>
-->
